# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Elixir CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  download_translations:
    runs-on: ubuntu-latest
    steps:
    - shell: bash
      env:
        DROPBOX_API_ACCESS_TOKEN: ${{ secrets.DROPBOX_API_ACCESS_TOKEN }}
        DROPBOX_CLIENT_ID: ${{ secrets.DROPBOX_CLIENT_ID }}
        DROPBOX_CLIENT_SECRET: ${{ secrets.DROPBOX_CLIENT_SECRET }}
        DROPBOX_CLIENT_REFRESH_TOKEN: ${{ secrets.DROPBOX_CLIENT_REFRESH_TOKEN }}
      run: |
        scripts/download.bash
    - name: Upload translation output
      uses: actions/upload-artifact@v4
      with:
        name: translation-output
        path: translation/ahcip.zip
  # Build job
  build:
    # Specify runner +  build & upload the static files as an artifact
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Elixir
      uses: erlef/setup-beam@v1
      with:
        elixir-version: '1.19.5' # [Required] Define the Elixir version
        otp-version: '28.1'      # [Required] Define the Erlang/OTP version
    - name: Restore dependencies cache
      uses: actions/cache@v3
      with:
        path: deps
        key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
        restore-keys: ${{ runner.os }}-mix-
    - name: Install dependencies
      run: mix deps.get
    # - name: Run tests
    #   run: mix test
    - name: Download translations
      uses: actions/download-artifact@v5
      with:
        name: translation-output
    - name: Build site
      run: mix ahcip.build
    - name: Upload static files as artifact
      id: deployment
      uses: actions/upload-pages-artifact@v3 # or specific "vX.X.X" version tag for this action
      with:
        path: output/

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
