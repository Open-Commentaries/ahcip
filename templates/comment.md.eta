@<%= it.urn %>

:authors: <%~ it.authors.map(author => author.username).join(', ') %>

:citation: <%= it.citation %>

<% if (it.tags.length > 0) { %>
:tags: <%~ it.tags.map(tag => tag.name).join(', ') %>
<% } %>


# <%~ it.title %>


<%~ it.text %>